<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /admin/

# Allow direct access to PHP files
RewriteCond %{THE_REQUEST} ^[A-Z]+\s/+admin/.*\.php [NC]
RewriteRule ^ - [L]

# Your other rules...
</IfModule>